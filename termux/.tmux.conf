# Or move to: ~/.config/tmux/tmux.conf

# Remap zoom to Alt+z without prefix
bind-key -n M-z resize-pane -Z

# # Split panes with Alt+v (vertical) and Alt+h (horizontal)
# bind-key -n M-v split-window -h
# bind-key -n M-h split-window -v
#
# # Navigate panes with Alt+arrow-keys
# bind-key -n M-Left select-pane -L
# bind-key -n M-Right select-pane -R
# bind-key -n M-Up select-pane -U
# bind-key -n M-Down select-pane -D
#
# # Resize panes with Alt+Shift+arrow-keys
# bind-key -n M-S-Up resize-pane -U 5
# bind-key -n M-S-Down resize-pane -D 5
# bind-key -n M-S-Left resize-pane -L 5
# bind-key -n M-S-Right resize-pane -R 5
#
# # Swap panes with Alt+[ and Alt+]
# bind-key -n M-[ swap-pane -U
# bind-key -n M-] swap-pane -D
#
# # Break pane to new window with Alt+b
# bind-key -n M-b break-pane
#
# # Kill pane with Alt+x (no confirmation for speed)
# bind-key -n M-x kill-pane

